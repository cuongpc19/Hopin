{
  "name": "pixel-flow",
  "version": "0.1.0",
  "description": "Pixel Flow - conveyor color-shooter puzzle game (Phaser 3 + TS + Vite)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "sharp": "^0.35.3",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
