================================================================================
 HOP IN! (Pixel Flow) — GHI CHÚ TÍNH NĂNG
 File này liệt kê các tính năng đã làm + chỗ chỉnh trong code + key localStorage.
 (Cập nhật thủ công khi thêm/sửa tính năng.)
================================================================================


────────────────────────────────────────────────────────────────────────────
0) XE TỰ LÁI 🚗 — CHẠY TIẾP / TỰ LÊN LẠI  [ĐÃ TẮT 2026-07-25]
────────────────────────────────────────────────────────────────────────────
>>> ĐÃ TẮT (user 2026-07-25): auto-drive làm độ khó thành NHỊ PHÂN (~95% hoặc ~5%,
    không tune được winrate) -> phá hệ thống thiết kế level. Đặt AUTO_CIRCLE = false
    trong GameScene.ts -> game về hành vi CŨ: xe chạy hết vòng thì ĐỖ, BẤM để phóng
    lại. Telegraph + modal "Self-Driving Cars!" tự tắt theo cờ. Tester (build-levels.mjs)
    cũng đổi mặc định về MANUAL (AUTO_DRIVE = env AUTODRIVE==="1", mặc định false) ->
    số winrate/thiết kế level cũ có hiệu lực lại. Code auto-drive GIỮ NGUYÊN sau cờ để
    bật lại (AUTO_CIRCLE = true) nếu cần. Mô tả bên dưới là khi BẬT.

Mục tiêu (khi bật): người chơi KHÔNG phải bấm xe nhiều. Xe tự quản lý theo màu.
• Chạy hết vòng mà màu của xe VẪN CÒN ĂN ĐƯỢC (có slime cùng màu đang "lộ" —
  bắn tới được) -> xe cứ chạy tiếp, không về bãi. Chỉ về bãi khi không ăn được
  (màu bị chặn trong hoặc đã hết). Track vẫn tối đa 5 xe.
• Xe ở bãi TỰ NHẢY LÊN track khi màu của nó lộ trở lại (do xe khác bóc vòng
  ngoài) VÀ trên track chưa có xe cùng màu (hoặc cùng màu nhưng count=0).
• "Ăn được" = màu đang REACHABLE (ô đầu tiên tính từ mép hàng/cột là slime màu
  đó), KHÔNG phải chỉ tồn tại — nếu dùng "tồn tại" sẽ kẹt cứng (xe màu-bị-chặn
  chạy vòng vô ích, giữ chỗ). Reachability tính đúng như LOS thật (tia thẳng +
  2 tia chéo) và chỉ từ các cạnh track thực sự chạy.
• Nhóm xe liên kết (xe đôi=2, xe 3=3, …) CŨNG tự lái: cả nhóm chạy tiếp/lên lại
  nếu MỘT thành viên còn ăn được (cần đủ N ô trống để lên). Chỉ xe búa/gỗ giữ
  hành vi cũ (bấm tay).

  ► XE 3 / NHÓM N XE: N chest cùng "pairId", XẾP LIÊN TIẾP trong mảng chests,
    N ≤ 4 (perRow). Cả nhóm: phóng/chạy(khối cứng)/đỗ(N ô cạnh nhau)/rời cùng
    lúc; nối dây giữa các thành viên. Code: ChestView.group (thay cho .twin).
• Vẫn phải bấm để phóng xe LẦN ĐẦU từ hàng chờ (zone 3) — chỉ tự động phần
  phóng-lại-từ-bãi.
• BÁO TRƯỚC KHI TỰ LÊN (telegraph, đã làm RÕ HƠN): xe đỗ ở bãi khi biết còn
  slime ăn được sẽ NHÚN BẬT mạnh (bob -14px + phình ×1.18) + MŨI TÊN ⬆ xanh nảy
  phía trên + VÒNG vàng nhấp nháy quanh ô, ~0.52s để user thấy "nó sắp nhảy lên",
  RỒI mới phóng. Cả nhóm xe nhún cùng lúc. Hủy telegraph nếu mất việc (xe khác
  cùng màu ăn trước) / bấm tay / booster / tutorial / có xe khác về đỗ (reshuffle).
  Code: armBay / disarmBay / disarmAllBays + const AUTO_RELAUNCH_TELEGRAPH_MS.
• GIỚI THIỆU 1 LẦN: [ĐÃ GỠ 2026-07-25] modal "Self-Driving Cars!" (maybeShowAutoDriveIntro
  / showAutoDriveIntroModal) đã bị XOÁ khỏi GameScene.ts vì auto-drive đã tắt — không
  cần dạy nữa. Nếu bật lại AUTO_CIRCLE thì phải viết lại tutorial này (không còn code).
  Key pf_autodrive_intro không còn dùng.
• ROLLBACK: đặt AUTO_CIRCLE = false trong GameScene.ts để về hành vi cũ.
  Code: computeReachableColors / canKeepCircling / autoRelaunchBays (GameScene.ts).

KEY localStorage: pf_autodrive_intro = đã xem modal giới thiệu xe tự lái chưa.


────────────────────────────────────────────────────────────────────────────
1) LUCKY CLOVER 🍀 — SỰ KIỆN THU THẬP
────────────────────────────────────────────────────────────────────────────
Mục tiêu: khuyến khích người chơi vượt nhiều level. Thắng level -> nhận Clover.
Tích đủ theo từng mốc -> tự nhận thưởng (Gold / Booster). Clover KHÔNG bị tiêu,
chỉ là chỉ số tiến trình. (Trước tên là "Treasure Rock" — đã đổi thành Lucky Clover.)

• Mở khoá: sau khi THẮNG Level 12 (event hiện tới khi nhận hết mọi mốc).
  (Đổi mốc mở: EVENT_UNLOCK_LEVEL trong cloverEvent.ts.)
• Clover mỗi lần thắng:
    - Thắng ngay lần đầu (first clear + KHÔNG thua/không chơi lại lần đó) = 2 Clover
    - Thắng kiểu khác (đã thua rồi thắng lại, hoặc chơi lại màn đã qua)     = 1 Clover
    - Thua = 0 Clover
• 30 mốc, ngưỡng Clover cộng dồn TĂNG DẦN:
    - Mốc 1–10 : +10 mỗi mốc  (~5 lần thắng first-try)
    - Mốc 11–20: +13 mỗi mốc  (~6–7 lần)
    - Mốc 21–30: +17 mỗi mốc  (~8–10 lần)
    (Tổng để hoàn thành ~400 Clover.)
• Phần thưởng xen kẽ:
    - Mốc lẻ  -> Gold (300, 400, 500, ... tăng 100 mỗi mốc gold)
    - Mốc chẵn -> Booster (xoay vòng Add -> Shuffle -> Grab -> Magnet)
    - Mốc 30  -> Grand Reward (3000 Gold + Magnet)
• Booster nhận từ event: tự +1 vào kho VÀ mở khoá luôn (dùng được kể cả chưa tới
  level mở khoá bình thường).
• Home: thanh sự kiện kiểu "Sunny Banner" (thẻ xanh + ruy-băng vàng "LUCKY CLOVER",
  huy hiệu 🍀, thanh chia đốt/pip, ô preview thưởng kế). Bấm vào thanh -> popup lộ
  trình 30 mốc (✅ đã nhận / ➡️ mốc kế phát sáng / 🔒 khoá), icon thưởng thật.
• Sau mỗi màn thắng: màn Victory hiện "+X 🍀 Lucky Clover", phần thưởng vừa mở (nếu
  có), và "còn N nữa -> phần thưởng kế".

CHỖ CHỈNH:
  • Logic + số liệu:  src/game/cloverEvent.ts
      - incrementFor()   -> đổi số Clover mỗi mốc
      - MILESTONES       -> đổi phần thưởng từng mốc
      - EVENT_UNLOCK_LEVEL = 10 ; EVENT_NAME = "Lucky Clover" ; CLOVER_ICON = "🍀"
      - cloversForWin()  -> đổi 2/1 Clover
  • Trao Clover + thưởng khi thắng: src/scenes/GameScene.ts -> win() (grantEventReward/grantEventBooster)
  • Cờ "thua/chơi lại màn này":      GameScene.failedThisAttempt (reset ở create, set true ở lose)
  • Thanh + popup ở Home:            src/scenes/LevelSelectScene.ts -> buildEventBar(), showEventRoadmap(),
                                     drawCloverMedallion(), drawRewardIcon()

KEY localStorage:
  pf_event_rocks    = tổng Clover (giữ tên key cũ để không mất tiến trình đã lưu)
  pf_event_claimed  = số mốc đã nhận
  (mở khoá suy ra từ pf_progress > 10)


────────────────────────────────────────────────────────────────────────────
2) GOLD (tiền tệ)
────────────────────────────────────────────────────────────────────────────
• Bắt đầu = 0. Có reset-một-lần khi vào game (key pf_gold_reset) để dọn gold test cũ.
• Thắng level LẦN ĐẦU  = +50 gold (chơi lại KHÔNG được nữa -> chống farm).
• Nút "+" trên HUD     = +100 gold mỗi lần (đang mở, KHÔNG giới hạn — chỉ để test).
  (Muốn giới hạn lại: xem buyGold() — trước có bản chặn 10 lần bằng key pf_freebuys.)
• Icon coin: đĩa vàng viền vàng đậm, KHÔNG có chữ. Giống nhau ở Home và trong game.
CHỖ CHỈNH: GameScene.ts -> addGold(), buyGold(), win() (reward=50); coin ở HUD (~dòng "coin").
KEY: pf_gold, pf_gold_reset, pf_freebuys (nếu bật lại giới hạn)


────────────────────────────────────────────────────────────────────────────
3) BOOSTER
────────────────────────────────────────────────────────────────────────────
• 4 loại + mở khoá theo level + tặng free khi mở:
    Add=300 (Lv6) · Grab=500 (Lv11) · Shuffle=600 (Lv16) · Magnet=600 (Lv21)
    Tặng free (FREE_GIFT)=3 cái khi tới level mở khoá.
• Hết hàng (badge đỏ 0) -> chạm vào hiện hộp "Buy?" -> mua bằng gold, +1 vào kho ->
  chạm lần nữa để dùng (mua tách khỏi dùng, tránh mua hụt).
• KHÔNG còn chữ tên dưới icon (chỉ còn badge số + giá khi hết).
CHỖ CHỈNH: GameScene.ts -> BOOSTERS[] (giá/level), promptBuyBooster(), drawBoosters(),
           runBooster(), consumeBooster/canUseBooster/affordToast.
KEY: pf_boost_counts, pf_boost_gifted


────────────────────────────────────────────────────────────────────────────
4) BOOSTER MAGNET (cách chơi)
────────────────────────────────────────────────────────────────────────────
• Bấm Magnet -> chạm 1 slime -> hút CỤM slime cùng màu NỐI LIỀN theo cạnh
  (flood-fill 4 hướng), KHÔNG hút hết mọi slime cùng màu trên bàn.
• Chạm "rộng lượng": chạm gần là bắt ô gần nhất trong ~1.3 ô (slime nhỏ vẫn dễ chọn).
• Đã BỎ zoom camera lúc chọn (từng gây kẹt/đơ game).
CHỖ CHỈNH: GameScene.ts -> boosterMagnet(), nearestSlime(), connectedSameColor(),
           confirmMagnet(), spawnVipCollector().


────────────────────────────────────────────────────────────────────────────
5) ÂM THANH
────────────────────────────────────────────────────────────────────────────
• Tiếng "pop" khi xe bật khỏi khay: pop bật LÊN (320->880Hz) + "ting" nhẹ. (playPop)
• Nhạc nền đã bỏ; có SFX ăn slime (Audio.board) và xe đầy (Audio.finish).
CHỖ CHỈNH: GameScene.ts -> playPop(); src/game/audio.ts.


────────────────────────────────────────────────────────────────────────────
6) SETTINGS: CHỌN LEVEL — TUẦN TỰ / TỰ DO
────────────────────────────────────────────────────────────────────────────
• Nút ⚙ ở Home mở popup Settings. Có option "Level Select":
    - Sequential (MẶC ĐỊNH): chỉ chơi được các level đã tới; level chưa tới bị KHÓA
      (node xám + 🔒, chạm vào báo "Locked — finish the levels in order").
    - Any Level: chọn/chơi level bất kỳ.
• Đổi option -> lưu + rebuild map ngay (khóa cập nhật liền).
CHỖ CHỈNH: src/scenes/LevelSelectScene.ts -> openSettings(), freeSelect()/setFreeSelect(),
           makeNode() (style khóa), tapAt() (chặn khóa).
KEY: pf_freeselect ("1"=tự do, "0"/không có=tuần tự)


────────────────────────────────────────────────────────────────────────────
7) NGÔN NGỮ & RESET (dev/test)
────────────────────────────────────────────────────────────────────────────
• Toàn bộ chữ trong game = Tiếng Anh.
• Reset máy đang test bằng URL:
    ?reset=all  -> xoá HẾT (gold, tiến trình, booster, tutorial, event)
    ?reset=1    -> chỉ bật lại tutorial (giữ gold + tiến trình)
  (Xử lý trong src/main.ts; tự bỏ tham số khỏi URL sau khi chạy.)
• (Remote reset "xoá mọi user khi online" đã LÀM RỒI XOÁ — không còn trong code.)


────────────────────────────────────────────────────────────────────────────
7b) VERSION TRÊN MÀN START (để biết đã load bản mới)
────────────────────────────────────────────────────────────────────────────
• Màn Splash (start) hiện ở đáy: "v0.0.<N> · <hash>".
• TỰ TĂNG theo số commit git: version = 0.0.<git rev-list --count HEAD>, kèm short
  commit hash. Mỗi lần commit/push (deploy) là số tự tăng — KHÔNG cần sửa tay.
  Nếu không có git thì fallback về package.json version.
• Netlify clone repo có git nên tính đúng lúc build. Sau deploy, mở lại trang thấy
  số/hash đổi = đã load bản mới.
CHỖ CHỈNH: vite.config.ts -> buildVersion() + define(__APP_VERSION__/__APP_BUILD__);
           src/global.d.ts (khai báo); src/scenes/SplashScene.ts (hiển thị).


────────────────────────────────────────────────────────────────────────────
8) GAME FEEL — TỐC ĐỘ XE / ĂN SLIME / ÂM / LAYOUT (2026-07-24)
────────────────────────────────────────────────────────────────────────────
• Xe chạy +30% (SPEED 10->13).
• Ăn slime nhanh ~2.25x, đều "như điện giật" (SHOT_COOLDOWN 180->80ms);
  slime chạy tới xe nhanh hơn cho khớp (RUN_MAX 460->700, RUN_ACCEL 560->850).
• Đoạn cuối (không còn xe xếp hàng — queueEmpty): xe chạy GẤP ĐÔI (drive x2)
  + gắp nhanh (cooldown/3, gắp hết slime trong tầm mỗi tick).
• Slime chạm xe: phồng "bụp" x1.7 (Back.out) trước khi biến -> đã mắt.
• Âm: Audio.pop() — pop nhỏ nhẹ mỗi lần ăn slime (nhẹ hơn tiếng cũ).
• Layout: ray vuông ôm gần hết bề ngang (edge 4px); cụm dưới ghim sát đáy;
  slime to hơn ~15% (bỏ trần board 0.56).
CHỖ CHỈNH: GameScene.ts (SPEED, SHOT_COOLDOWN, RUN_MAX/ACCEL, freeAdvance boost,
           buildBoard/getBoardMetrics layout); src/game/audio.ts (pop()).


────────────────────────────────────────────────────────────────────────────
9) THIẾT KẾ LEVEL — build tự động (scripts/build-levels.mjs + CSV)
   CSV: Manythings/Design winrate/winratedesign1.csv  (chạy --config)
────────────────────────────────────────────────────────────────────────────
Cột CSV (input): lvl, tier, target, max màu, maxxe, minxe, xedoi, track,
                 max slim, skill   | (ref build ghi: slime_ref, win_ref,
                 xe_ref, màu_ref, note)

Quy tắc:
• Winrate đo bằng bot "người chơi trung bình" ở SKILL RIÊNG mỗi level
  (1=hoàn hảo, 0.7-0.8=giỏi, 0.6=TB, 0.4=gà). Build căn winrate ~ target.
• TARGET (2026-07-24): target>=50 -> +10 (cap100); super(mốc15)<=25;
  hard từ L20 (mốc5,>=20) <=40.
• COUNT/xe (slime mỗi xe): cho phép 1-40, ƯU TIÊN 15-35, ideal ~25.
  minxe (sàn) ưu tiên hơn maxxe (trần).
• Độ khó từ: số MÀU + thứ tự CHÔN + XE ĐÔI (không nhồi xe con).
    - quá khó -> tự giảm màu;
    - hard/super còn dễ -> tự THÊM XE ĐÔI (trần hard 4 / super 6 cặp);
    - dễ mà kẹt trần -> note để nâng max màu.
• Chủ thể: to nhất trong giới hạn max slim (L1-2=200 nhỏ; L3-100=750 to full).
• Phủ slime: từ L3 phủ >=70% board; thiếu thì vẽ VIỀN (nở 2 màu quanh chủ
  thể tới 70%); hình vuông/chữ nhật cũng được.
• LEVEL XẢ HƠI: ngay sau mỗi hard/super (L6,11,...,96 = n%5==1): siêu dễ
  (~95-100%), chủ thể to, ~6 màu -> lấy slime liên tục, đã tay.
• Track: tất cả "square". Solver kiểm mọi level giải được (kể cả xe đôi).

Lệnh:
  build cả 100:  node scripts/build-levels.mjs --config "<csv>"
  rebuild vài:   thêm --only 3,5,20   (giữ nguyên level khác + tranh)
  đo winrate:    --report --config    -> ghi report.json (skill riêng, 40 lần)
  vòng lặp: build -> --report -> auto-tune CSV chỗ lệch -> --only rebuild.
  designed.json = file game đọc trực tiếp (điện thoại đóng/mở lại tab để nạp).


────────────────────────────────────────────────────────────────────────────
10) CƠ CHẾ SLIME 2 LỚP + SLIME "?" + XE BA (2026-07-24)
────────────────────────────────────────────────────────────────────────────
• SLIME 2 LỚP (level.layer2[i] = màu ẩn dưới ô i): ăn lớp trên -> slime màu
  khác pop ra ở đúng ô đó (dấu góc gập trắng đánh dấu ô 2 lớp). Xe được cấp
  thêm capacity cho lớp dưới. Có ở hard/super từ L35 + toàn bộ pack L101-120.
• SLIME "?" (level.hidden[i] = màu thật): slime ĐEN + dấu ? trắng TO ĐẬM
  (2026-07-25: đổi từ xám nhỏ sang đen/to cho dễ nhìn), KHÔNG bắn được khi
  còn ẩn; mở 1 ô cạnh -> lộ màu thật (pop). board vẫn giữ màu thật (solver
  đúng); chỉ là ẩn với người chơi. Cùng phạm vi level như 2 lớp.
• XE BA (3 xe khác màu chung pairId): logic như xe đôi (đi/đỗ/rời cùng nhau,
  cần 3 khay cạnh nhau). Game side đã hỗ trợ nhóm N từ trước; build đặt ở
  L20,25,30,45 + pack (mỗi level chia hết 3).
• 🐛 FIX DÂY NỐI xe đôi DÀI (2026-07-26, user L303): drawTwinLinks vẽ dây tới xe đôi
  ở HÀNG SAU BỊ CHE KHUẤT (ngoài viewport hàng chờ, y=1188 > GAME_H) → dây kéo ngang
  ~434px xuống xe vô hình. (Lần đầu thử cap theo cỡ xe *1.6 → QUÁ CHẶT, ẩn cả dây khi 2
  xe thấy được → user báo "k thấy dây".) SỬA ĐÚNG: bỏ qua nếu 1 xe bị CLIP dưới
  invMaskBottom (che khuất) → không nối tới xe vô hình; 2 xe ĐỀU THẤY thì vẫn vẽ (giữ
  ngưỡng rộng GAME_W*0.95). invMaskBottom = biên dưới mask hàng chờ (lưu ở buildInventory).
  ⚠ GỐC RỄ (không sửa, user chốt "để như cũ"): ~17/30 level rock-wall L301-330 có xe đôi
  đặt SÂU (row≥3) vì pickGroups đặt cặp ở hàng nông nhất còn SOLVABLE — level tường/màu-dồn
  làm hàng nông unsolvable → đẩy sâu. Rule "twin nông" mâu thuẫn solvability ở các level này.
  User chọn GIỮ NGUYÊN placement, chỉ cần fix hiển thị dây (đã xong). Đừng ép rule/rebuild.
• ⭐ RULE ĐỘ SÂU XE ĐÔI (2026-07-26, user L9): nhóm liên kết PHẢI nằm trong 3 HÀNG ĐẦU
  của hàng chờ (rows 1-3) để dây LUÔN thấy. Game mask hàng 4+ rồi bỏ dây tới xe bị clip →
  xe đôi chôn sâu = KHÔNG có dây. User: "xe đôi khác hàng thì xe sâu nhất tối đa hàng 3"
  (1 xe hàng 4 nối chéo xuống xe hàng đầu → dây khuất). ĐÃ SỬA pickGroups: cap cả vòng đặt
  cặp (same-row) lẫn vòng xe-ba vào VISIBLE_ROWS=3 (rowCap=3*perRow); không nhét được nông
  thì đặt ÍT xe đôi hơn chứ không chôn sâu. Rebuild L9/20/30/35/44/45 (--only) → mọi nhóm
  giờ ở hàng 1-3, winrate vẫn bám target. (Khác với ca L301-330 ở trên: bộ 45 chấp nhận
  bớt xe đôi để giữ dây thấy; bộ rock-wall thì ưu tiên solvability.)
• ⭐ XE ĐÔI THẲNG HÀNG (CÙNG CỘT) CHÔN SÂU ĐƯỢC (2026-07-27, user): ngoại lệ của rule
  trên — nếu 2 xe đôi Ở CÙNG CỘT, xếp TRÊN-DƯỚI liền nhau (không xe lạ chen giữa) thì
  dây là link DỌC ngắn, KHÔNG BAO GIỜ lệch nhau (cùng 1 cột rút đều) → chôn sâu bao
  nhiêu cũng thấy dây khi nổi lên. CHỈ ca khác-cột (ngang) mới bị lệch→chéo→khuất.
  ĐÃ TRIỂN KHAI 3 chỗ ĐỒNG BỘ:
  1) GAME launchFromInventory: điều kiện launch nhóm đổi từ "mọi xe ở hàng đầu (r=0)"
     → "không xe LẠ nào đứng TRƯỚC nó trong cột" (front-prefix). Cặp cùng cột rows 0,1
     launch cả cụm. (p.col.slice(0,p.r).every(∈group))
  2) SIM: solvablePairs & playAverage thêm groupReady(g) = mỗi xe parked HOẶC không có
     xe-lạ đứng trước trong cột; removeCar đổi shift(col[0]) → indexOf+splice (xoá xe ở
     bất kỳ độ sâu). Thay hết các check g.every(isFront). groupReady ≡ isFront cho cặp
     ngang-nông (verify: L10/20/30 winrate KHÔNG đổi 63/61/46).
  3) BUILDER pickGroups: thêm vòng đặt cặp DỌC (idx a & a+perRow = cùng cột, sâu kề)
     ở BẤT KỲ độ sâu, chạy sau vòng same-row-nông. Test ép L30 xedoi=6 → 2 cặp tự xếp
     dọc row 4-5, level vẫn solvable. Vòng same-row vẫn cap 3 hàng (ngang phải nông).
  ⇒ Builder giờ đặt xe đôi nông-ngang TRƯỚC, hết chỗ thì dọc-sâu (thay vì bỏ bớt).
  • ÉP xe đôi dọc-sâu: env VERT_DEEP=N → floor số twin ≥N (n≥8) + pickGroups đặt N cặp
    DỌC ở slot SÂU NHẤT solvable (row≥2, quét deep→shallow) TRƯỚC vòng nông. ĐÃ ÁP L20:
    VERT_DEEP=1 → 1 cặp dọc sâu row 6-7, solvable 1/1, winrate 65→59 (twin sâu làm khó
    thêm ~2-6đ, tuner không kéo hết về target). VERT_DEEP=2 làm L20 quá khó (52) → dùng 1.
    ⚠ Level CHẬT (ít xe + nhiều twin sẵn, vd L10 16 xe 4 twin) ép dọc-sâu → greedy-solver
    KẸT (0/1) dù bot vẫn thắng → chỉ ép ở level RỘNG (≥~30 xe). Các level 45 khác GIỮ NGUYÊN.
• PACK L101-120 (advanced): tất cả hard/super, đủ cơ chế (2lớp + ? + đôi/ba),
  skill 0.9 (101-110), 0.8 (111-115), 0.98 (116-120; 1.0 là bot tất định nên
  dùng 0.98), target 15-20%%. Winrate thực đo: 8-28%% cả 20 level.
• BÀI HỌC CĂN CHỈNH: ở skill >=0.9 winrate là "vách đá" (0 hoặc 100) — căn
  bằng SEED SWEEP: build lại với --seedoff K (đổi thứ tự chôn xe), đo bằng
  --test1 n, giữ seed rơi vào 8-35%%. Script: scratchpad/seedsweep.sh.
CHỖ CHỈNH: src/game/level.ts (layer2/hidden), GameScene.ts (markTwoLayer,
  makeHiddenKey, revealHiddenAround, fireTo), scripts/build-levels.mjs
  (makeLayer2, makeHidden, pickGroups deep-first, --seedoff, --test1).


────────────────────────────────────────────────────────────────────────────
11) KID PACK L200-300 (2026-07-24) — pack cho bé
────────────────────────────────────────────────────────────────────────────
>>> BUILD LẠI 2026-07-25: toàn bộ L200-300 đã REBUILD thành LEVEL ẢNH 35×35 kiểu
    mục 13 (build-one.mjs whole-image), KHÔNG còn dùng pipeline build-levels.mjs
    (tuner/xe đôi/viền xanh) mô tả bên dưới. Mỗi level = 1 ảnh mosaic 35×35 (~1089
    slime, ~110-115 xe, 8 màu + nền), ảnh xoay vòng từ 48 ảnh (easylevel + hard).
    Lệnh đã chạy (tuần tự, ghi đè designed.json từng level):
      for lvl in 200..300: BOARD=35 node scripts/build-one.mjs "<ảnh>" -1 $lvl 8
    Mô tả pipeline cũ (tuner) bên dưới giữ lại để tham khảo / nếu muốn dựng lại.

• 101 level DỄ (L200-300) cho con của user chơi. Độ khó lặp CHU KỲ L2->L8
  (target 95/95/70/60/100/90/100, skill 0.4-0.7), tier luôn "normal".
• Chủ thể to hết cỡ (max slim 750), ảnh dễ thương từ easylevel.
• VIỀN chỉ dùng màu XANH dịu mắt: COOL_IDS = [3 xanh lá, 4 xanh ngọc,
  5 xanh dương, 15 xanh nhạt] (addOuterLayers nhận tham số palette).
• Xe đôi MỌI level (1-2 cặp theo chu kỳ); xe ba mỗi 10 level (206,216,...,296).
• Thi thoảng: slime 2 LỚP mỏng 10%% (mỗi 4 level: 202,206,...), slime "?" 8%%
  (mỗi 5 level: 203,208,...). Vẫn dễ — tuner đo kèm layer2 nên target giữ nguyên.
• Config: các dòng lvl 200-300 (tier "kid") trong winratedesign1.csv.
• Game side: levelDifficulty() trả "normal" cho 200-300 (không badge 💀/🔥);
  LevelSelectScene LEVEL_COUNT 60 -> 300. L121-199 là gap (procedural fallback);
  bé vào pack bằng Settings ⚙ -> Any Level rồi chạm L200, hoặc mở ?level=200.
• Build lại riêng pack:  node scripts/build-levels.mjs --config
  "Manythings/Design winrate/winratedesign1.csv" --only 200,...,300
• KẾT QUẢ ĐO (2026-07-24, 40 lượt/level): 101/101 lệch ≤10 điểm so target
  (95/101 trong ±8). Tổng 130 xe đôi, 10 level xe ba, 25 level 2-lớp,
  20 level "?". 12 level lệch ban đầu sửa bằng nudge màu + seed-sweep
  (--seedoff); refs đã ghi vào CSV.

12) NHẢY THẲNG TỚI LEVEL + TINH CHỈNH (2026-07-24)
────────────────────────────────────────────────────────────────────────────
• Settings ⚙ có mục "Jump to Level": chạm "Enter Level Number" -> nhập số
  (1–300) -> vào chơi ngay level đó. Không cần cuộn map. makeLevel() có
  fallback procedural nên số nào cũng chơi được. (LevelSelectScene.openSettings)
• Màn Victory / Out of Space giờ chờ 1 nhịp (750ms / 650ms) mới hiện, thay vì
  bật ngay khi ván kết thúc — đỡ giật. (GameScene showWinModal/showLoseModal)
• Fix: launch nhóm xe liên kết (xe đôi / xe 3) không còn spawn đè lên xe đang
  chạy vòng. Trước chỉ kiểm tra node xuất phát; giờ kiểm tra CẢ dải node nhóm
  sẽ chiếm (xe 3 chiếm tới +10 node). (GameScene.trySpawn)

13) PICTURE-LEVEL — BUILD BOARD TỪ ẢNH NHÂN VẬT (mosaic) (cập nhật 2026-07-25)
────────────────────────────────────────────────────────────────────────────
>>> ĐỌC MỤC NÀY để hiểu CÁCH BUILD BOARD picture-level. Đây là quy trình chuẩn.

Ý tưởng: các slime ghép lại thành hình 1 nhân vật (như tranh cross-stitch). Ngoài
pack chuẩn 25×25, board có thể TO hơn (31/39) cho mosaic mịn hơn.

CÔNG CỤ: scripts/build-one.mjs — patch THẲNG 1 level vào src/levels/designed.json
(KHÔNG đụng level khác). Preview PNG: scripts/_level-preview/one-L<level>.png.

LỆNH:
  node scripts/build-one.mjs <ảnh> <cell> <level> <K> <SIZE>
    <ảnh>  : PNG nhân vật đã cắt (nền trong suốt).
    <cell> : -1 = "whole-image" (file 1-nhân-vật, KHUYÊN DÙNG).
             0..9 = cắt 1 ô của ảnh lưới sprite 5×2 (0-4 hàng trên, 5-9 hàng dưới).
    <level>: số level cần ghi.
    <K>    : số MÀU THẬT = con số đầu tên file (vd 6_train_toys.png -> K=6).
    <SIZE> : cỡ board. 25=slime CHUẨN (như level thường), 31=nét vừa (KHUYÊN DÙNG
             cho đại trà), 39=nét nhất (nhưng ~140 xe/level, rất dài). Cũng đặt được
             qua env BOARD; đối số positional thắng khi có cả hai.
  Vd:  node scripts/build-one.mjs "public/art/level art/sliced/_hard/6_train_toys.png" -1 501 6 31

THƯ VIỆN NHÂN VẬT (đã cắt sẵn + đặt tên "{sốmàuthật}_{tên}_{bộ}.png"):
  public/art/level art/sliced/_simple/     (≤4 màu  -> xếp vào level DỄ)
  public/art/level art/sliced/_hard/       (5-6 màu -> level KHÓ)
  public/art/level art/sliced/_superhard/  (≥7 màu  -> level SIÊU KHÓ)
  (bản gốc theo bộ: sliced/{toys,flashcard,heroes,animals2,objects}/ và art/level art/1/)
  Quy tắc: ÍT màu -> level dễ, NHIỀU màu -> level khó.

RECIPE (build-one.mjs tự làm hết — KHÔNG cần sửa):
  1. Cắt nền: flood 4 góc (backgroundMask) bỏ nền trong suốt + màu góc; crop sát chủ thể.
  2. Hạ mẫu xuống lưới SIZE-2 (MARGIN=1 chừa 1 ô viền); canh giữa board SIZE×SIZE.
  3. Snap màu "trung thực + tươi": gom về K màu dùng nhiều nhất trong palette BRIGHT
     = 16 màu (19 TRỪ 3 màu xỉn 13/16/18). VẪN GIỮ đen/trắng/xám/nâu/tan -> penguin
     đen, gấu nâu ĐÚNG màu, không bị lệch xanh.
  4. Full-fill nền (FILL_BG mặc định bật): lấp TOÀN BỘ ô trống trong khung bằng 1 màu
     tương phản nhất (BRIGHT trừ trắng 8 + tan 14, chọn theo max-min distance tới mọi
     màu chủ thể). -> chủ thể lấp kín khung, nền 1 màu sạch, ~84.6% ô có slime (25×25),
     chừa đúng 1 ô viền cách đường ray. (Ép nền cố định: env BG_ID=<id>.)

IN-GAME (ĐÃ XONG, ĐỪNG sửa GameScene sizing):
  slime TỰ CO theo SIZE: STD=Math.max(25,cols,rows) -> cell nhỏ lại khi board>25;
  capCell chặn cols*cell không vượt vùng trong ray -> KHÔNG BAO GIỜ TRÀN đường ray.
  25=slime chuẩn, 31/39 nhỏ dần vừa khít. Mỗi level tự mang cỡ qua cols/rows.
  Chỗ: GameScene.ts ~L754 (STD), L758 (cell), L761 (capCell), L1021 (setDisplaySize).

KIỂM TRA: LUÔN mở preview scripts/_level-preview/one-L<level>.png xem hình có đọc
  được không. designed.json được bundle + HMR full-reload; sau build mở đúng số
  level (?level=NNN) để xem (map Home chỉ vẽ tới 300 node).

ĐIỂM YẾU ĐÃ BIẾT:
  • Chủ thể THÂN TRẮNG (vd panda) bị flood nhầm thành nền -> hỏng (thân trắng rò ra
    viền trắng ngoài). TRÁNH dùng, hoặc soi preview kỹ.
  • Xe (chests) = generateChests đơn giản (chia mỗi màu cụm 8-12, cap≈slime nên
    winnable) — CHƯA qua tuner win-rate như pack chuẩn. Độ khó/solvability chưa
    đảm bảo; sẽ có tuner riêng sau.
  • Level cũ (vd L25) build bằng recipe CŨ nên còn khung trắng (nền thẻ bị giữ) —
    build lại bằng recipe này sẽ ra nền 1 màu tương phản sạch.

HIỆN CÓ: L500-506 = các bản test train/cat/penguin ở 25/31/39 (chọn cỡ mặc định).

────────────────────────────────────────────────────────────────────────────
14) CÔNG THỨC LEVEL SUPER HARD (note theo yêu cầu user, 2026-07-25)
────────────────────────────────────────────────────────────────────────────
SUPER HARD ở đâu:
• Campaign chính: mỗi level 15 (L15,30,45,...,90). Target 25%%; riêng các mốc
  30 (L30/60/90) là "brutal" target 10%%. Đo ở skill 0.75-0.8 (L15 ở 0.8).
• Advanced pack L101-120: mỗi level chia hết 5 (105,110,115,120) là super,
  target 15-20%%, skill cao 0.8/0.9/0.98.
• Ngay SAU mỗi hard/super luôn có level thở (n%%5==1): target ~100, 6 màu.

6 ĐÒN BẨY ĐỘ KHÓ (xếp theo thứ tự nên dùng):
1. NHIỀU MÀU: đặt "max màu" trong CSV cao — TRẦN CỨNG 12 (user 2026-07-25:
   quá 12 màu nhìn rối; build clamp MAX_COLORS=12, CSV cũng đã hạ hết về 12).
   Nhiều màu = xe khó khớp lớp ngoài -> phải đỗ chờ. Build coi đây là TRẦN:
   nếu quá khó nó tự giảm màu từng nấc (reduceColors gộp màu hiếm vào gần nhất).
2. CHÔN XE (burial): xe màu vòng NGOÀI bị đẩy sâu trong hàng chờ (bias 0->0.78,
   calibrateOrder sweep tự chọn). Người chơi phải đỗ xe "sai màu" vào 5 ô bãi
   để mở cột -> áp lực tắc bãi = nguyên nhân thua chính.
3. XE ĐÔI + XE BA: nhóm phải phóng cùng lúc + cần N ô bãi CẠNH NHAU để đỗ ->
   dễ nghẽn. Super có 3 đôi + 1 ba (vd L30). Nếu hard/super vẫn QUÁ DỄ, build
   TỰ THÊM xe đôi từng cặp (trần: super 6 đôi, hard 4 đôi). Nhóm được đặt
   SÂU cuối hàng chờ (deep-first) để không wedge level.
4. SLIME 2 LỚP: phủ ~20-25%% chủ thể (patch giữa bàn, patch to chia 2 màu đáy).
   Ăn lớp trên -> lòi màu khác = tải thêm mà không nhìn thấy trước.
5. SLIME "?": ~12-14%% ô trong bị che -> không lập kế hoạch trước được.
6. COUNT XE 15-35 slime/xe (ideal ~25): độ khó KHÔNG đến từ việc nhồi xe
   hay màu bừa (xe nhiều = dễ hơn vì nhiều lần phóng; màu nhồi = bàn lem).

QUY TRÌNH (lặp cho tới khi đạt target):
① Ghi dòng level vào winratedesign1.csv: target / max màu / maxxe / minxe /
   xedoi / skill. ② Build: node scripts/build-levels.mjs --config
   "Manythings/Design winrate/winratedesign1.csv" --only N. ③ Đo 40 lượt:
   --test1 N (hoặc --report cả file, tự ghi report.json). ④ Lệch thì chỉnh:
   quá DỄ -> tăng màu / thêm xe đôi; quá KHÓ -> giảm màu / tăng minxe.
⑤ SKILL >= 0.9 (pack): winrate là "vách đá" 0%%/100%%, chỉnh knob bị lật
   ngẫu nhiên -> dùng SEED-SWEEP: build lại với --seedoff 1..5 (đổi thứ tự
   chôn xe), giữ seed đầu tiên đo lọt band. Đây là cách đã chốt cả pack
   L101-120 (8-28%%) và L10/L30.

LƯU Ý QUAN TRỌNG (2026-07-24): bot đo CHƯA mô phỏng xe tự lái -> winrate
thật CAO hơn số đo, lệch nhất ở hard/super (L30 đo 10%% mà chơi thấy dễ).
Viền 2 màu phủ rộng là "mồi" cho xe tự lái (luôn lộ ngoài cùng -> xe viền
tự quay vòng ăn sạch, không chiếm bãi). Đang chờ dạy bot ở session khác;
trước đó coi số đo là CẬN DƯỚI độ dễ. Muốn super khó thật ngay: giảm độ
phủ viền hoặc trộn viền 3-4 màu.

CHỖ CHỈNH: scripts/build-levels.mjs -> tuneToTarget (twinCap, vòng giảm màu,
  vòng thêm xe đôi), layer2FracFor / hiddenFracFor / triplesFor (phạm vi cơ
  chế), calibrateOrder + sweepCars (chọn bias/số xe), --seedoff / --test1 /
  --report; config: Manythings/Design winrate/winratedesign1.csv.

────────────────────────────────────────────────────────────────────────────
15) REBUILD TOÀN BỘ L1-120 THEO RECIPE PICTURE + KÍCH THƯỚC MỚI (2026-07-25)
────────────────────────────────────────────────────────────────────────────
• Cả 120 level build lại bằng RECIPE PICTURE (mục 13) ngay trong build-levels.mjs:
  ảnh nhân vật sliced/ (_simple ≤6 màu cfg / _hard 7-9 / _superhard ≥10), snap màu
  THẬT (giữ đen/trắng/nâu; bỏ 3 màu xỉn 13/16/18), nền 1 màu tương phản full-fill,
  margin 1 ô. KHÔNG viền trang trí, KHÔNG relief-reduce, KHÔNG maxSlime (cột
  "kích thước" quyết định số slime).
• KÍCH THƯỚC per-level từ CSV cột 15: L1-2=25, L3-10=31, L11-104=35, L105-120=39.
  BOARD_SIZE giờ là biến (let), set mỗi level; game tự co slime (STD/capCell).
• L106-120 MỚI (user order): 15 level ultra, target 5%%/10%%, skill ghi 0.98
  (bot skill=1 là tất định -> đo 0/100, không căn được; 0.98 = sát 1 nhất đo được).
• KẾT QUẢ ĐO (40 lượt, bot auto-drive): 110/120 trong ±8 điểm target, CẢ 120
  trong ±10, 120/120 perfect-solver giải được. Max lệch 10.
• CÁCH CĂN (3 vòng): A) xe đôi CSV theo độ lệch; B) LỚP 2 ẨN qua env L2FRAC
  (không phá tranh — chìa khóa cho level ít màu mà target thấp); C) seed-sweep
  --seedoff 1..5 (yêu cầu solvable + lọt band).
• ⚠ TRẠNG THÁI NGOÀI CSV (phải nhớ khi rebuild lẻ 1 level):
  - L2FRAC (lớp 2 ẩn) đã gắn qua env: 0.22 -> L18,24,27,37,57,63,64,74,78,80,87;
    0.18 -> L20; 0.10+L2POS=outer -> L25 (31×31, off1, IMG=6_train_toys, lớp 2 VÀNH NGOÀI); 0.12 -> L43,53,59,67,92,98,99; 0.1 -> L23,34;
    L2FRAC=0 (TẮT rule mặc định pack) -> L105,109,111,114,115,117,120
    (lớp 2 mặc định làm 39×39 không giải nổi).
  - Nhiều level chốt ở --seedoff ≠0 (22,45,48,53,58,62,70,80,87,88,90,97,99,100,
    102,105,109,111,115,117,120...). Rebuild lẻ KHÔNG kèm đúng env/seedoff sẽ
    làm lệch lại -> sau khi rebuild lẻ LUÔN đo lại --test1 và chỉnh.

────────────────────────────────────────────────────────────────────────────
16) LINE XẾP HÀNG (3/5 CỘT) + XE CHÔN "?" (2026-07-25, học từ game tham chiếu)
────────────────────────────────────────────────────────────────────────────
Ý TƯỞNG (user): game tham chiếu (Pixel Flow thật) làm board KHÔNG quá lớn mà
vẫn khó nhờ: (a) chôn rất nhiều xe — hàng sau úp "?" không biết màu; (b) số
line xếp hàng thay đổi (3 line / 5 line — ít line = ít lựa chọn = khó);
(c) xe đôi đặt 2 xe CẠNH NHAU CÙNG HÀNG cho đẹp (mình đã làm sẵn từ trước —
pickGroups ép same-row adjacent-columns); (d) viền nền quanh chủ thể MỎNG.

CƠ CHẾ MỚI TRONG GAME:
• Level.lanes (designed.json, mặc định 4): số cột hàng xếp. GameScene đọc
  this.level.lanes (clamp 2-6). Bot/solver mô phỏng ĐÚNG số cột đó (biến LANES
  trong build-levels.mjs, tự set per level ở build/--report/--test1/--diag).
• Chest.buried ("xe chôn"): xe úp tối + "?" vàng, GIẤU màu + số ghế; lật ngửa
  (revealBuried, pop nhỏ) khi lên ĐẦU cột. Hàng đầu tiên không bao giờ chôn.
  Xe đôi/ba chôn CẢ NHÓM hoặc không (giống "?" đôi cạnh nhau ở game tham chiếu).
  Bot coi như nhìn xuyên (nó vốn chỉ đọc màu xe ĐẦU cột — luôn đã lật) nên số
  đo winrate KHÔNG đổi: chôn là độ khó CỘNG THÊM cho người, như slime "?".
• CSV thêm cột 16 "line" (3/4/5) + cột 17 "chôn xe (0-1)" (tỷ lệ xe hàng sau
  bị chôn; nhận 0.6 hoặc 60 đều được). Env override khi build lẻ: LANES=, BURY=.

5 LEVEL TRÌNH DIỄN L121-125 — KẾT QUẢ CHỐT (đo 40 lượt @0.9, đều solvable,
ảnh gần vuông → viền 1-2 ô):
  L121 star    25×25 5-line chôn  7/22 xe → 15%  (--seedoff 5, IMG=7_star_heroes)
  L122 book    27×27 2-line chôn 15/24 xe → 15%  (--seedoff 4, IMG=7_book_toys)
  L123 doll    29×29 3-line chôn 17/27 xe → 13%  (--seedoff 0, IMG=7_doll_toys)
  L124 dino    31×31 3-line chôn 14/24 xe → 10%  (--seedoff 0, IMG=7_dino_animals1)
  L125 pencils 31×31 3-line chôn 22/32 xe → 15%  (--seedoff 3, IMG=7_pencils_toys)

⭐ QUY LUẬT ĐỘ KHÓ (rút ra sau playtest L121-125, quan trọng nhất):
  Yếu tố #1 = ẢNH phải XEN KẼ RĂNG LƯỢC, KHÔNG mảng to. Ảnh mảng to (sách,
  búp bê) mở màu "ào 1 lượt" -> xe hốt đầy khoang ngay -> khay không nghẹt ->
  DỄ dù chôn/line thế nào (user chơi thấy 122/123 dễ, 121/124 khó, 125 trung
  bình). Ảnh răng lược (sao/khủng long/tàu/bút chì) mở nhỏ giọt -> xe nằm ì
  chờ -> khay căng THẬT.
  Yếu tố #2 = ít line + chôn dày (combo game tham chiếu). #3 = đủ màu ≥7-8
  (ít màu như L125=5 màu -> luôn có màu mở -> bốc mù vẫn trúng -> chỉ trung bình).
  #4 = lớp 2 VÀNH NGOÀI cứu được ảnh mảng to (bóc xong lại lòi màu khác).
  User chỉ đạo: "ảnh mảng to thì GIẢM LINE + TĂNG CHÔN" -> đã hạ L122 3->2 line
  chôn .8, L123 5->3 line chôn .8.
  ⚠ ĐÁNH ĐỔI (L123): giảm line làm bot đo KHÓ hơn -> tuner TỰ HẠ MÀU (8->3) để
  kéo winrate về target 12 -> 3 màu lại làm DỄ cho người (yếu tố #3). Trên ảnh
  mảng to, "ít line + target thấp" xung khắc với "giữ nhiều màu". Nếu playtest
  thấy L123 vẫn dễ: nâng target (bớt ép hạ màu) hoặc đổi sang ảnh răng lược.
  ➡ THỰC RA CÁI GỐC: hiện "lộ màu trên board KHÔNG có ý nghĩa" (user) vì phóng
  xe sớm vô hại (nằm khay chờ, không mất gì). Muốn kỹ năng đọc board quyết định
  thật -> cần TRAY MODE (khay 1 chiều, không rút ra) — đang chờ user quyết.
Các level này cũng tự có slime "?" (54-102 ô) + lớp 2 (70-152 ô) từ rule pack.
⚠ NGOÀI CSV: seedoff + IMG ở trên phải kèm khi rebuild lẻ (như mục 15).
Số đo là ở góc nhìn bot NHÌN XUYÊN chôn — người chơi thật sẽ thấy KHÓ HƠN số đo
(chôn + "?" là độ khó cộng thêm không đo được). Chờ user playtest để chỉnh.
• Bàn 39×39 + lớp 2 ẩn = bot hoàn hảo cũng nghẹt bãi (unsolvable) — ĐÃ bỏ lớp 2
  ở pack; độ khó pack đến từ 39×39 + 8 màu + chôn xe + xe đôi/ba ở skill 0.98.

► BOT BIẾT "XOAY BÃI" (2026-07-25, sau khi tắt auto-drive): playAverage được dạy
  chiêu của user — bãi đầy thì đẩy 1 xe đỗ (màu bị chặn) lên ray chạy vòng không
  công (mảng circ, tối đa 3 xe) để nhường chỗ; xe chạy vòng vẫn ăn nếu màu mở ra,
  về bãi khi trống chỗ; gated theo skill tại 3 điểm nghẹt (solo cần chỗ / nhóm cần
  chỗ / kẹt cứng). Kết quả đo TĂNG mạnh và khớp cảm giác người chơi thật:
  L20/25/30 cũ đo 98-100%% (đúng như user thấy "vẫn dễ").
  ⚠ TOÀN BỘ win_ref đo trước bot-xoay-bãi đã LỖI THỜI — mới căn lại: L25
  (target 30 -> đo 25, off2, L2FRAC=0.14). 119 level còn lại chờ lệnh re-calibrate.

14) XẾP LẠI THỨ TỰ XE (OUTSIDE-IN) + XE ĐÔI (L1-120) (2026-07-25)
────────────────────────────────────────────────────────────────────────────
Sửa 2 lỗi thiết kế của các level tranh dày (user báo ở L10):
• XE PHÓNG LÊN K ĂN ĐƯỢC GÌ: trước 26/120 level có xe màu-bị-chôn nằm ở HÀNG ĐẦU
  hàng chờ -> phóng lên chạy vòng bắn hụt. Nguyên nhân: thứ tự xe không outside-in.
• XE ĐÔI TÁCH XA: 2 xe cùng cặp ở cột khác nhau, đặt sâu (hàng 7-8) -> khi chơi
  cột vơi không đều, xe 1 lên hàng đầu còn xe 2 kẹt hàng 3.
CÁCH SỬA: scripts/fix-queue-order.mjs — hậu-xử-lý designed.json (KHÔNG đụng script
  build mà session khác đang sửa). Với mỗi level:
  - Peel board -> xếp hạng màu outside-in theo (LỚP NHỎ NHẤT nổi lên, rồi lớp TB).
    Xếp xe theo thứ tự đó -> hàng đầu luôn là màu lộ ngay (layer 0). KQ: 0/120 level
    còn xe-ăn-hụt ở hàng đầu.
  - Xe đôi: chỉ GIỮ cặp có 2 màu nổi lên gần nhau (rank cách <=1), đặt KỀ NHAU CÙNG
    HÀNG; cặp lệch quá thì BỎ LIÊN KẾT (thành xe đơn). KQ L1-120: giữ 99 / tách 98,
    khoảng cách hàng giữa 2 xe cặp = 0 (luôn cùng hàng).
  - Chỉ đổi mảng chests (thứ tự + pairId); count/board/track giữ nguyên -> slime ==
    sức chứa xe không đổi. Chạy lại được: node scripts/fix-queue-order.mjs 1 120
LƯU Ý: xếp outside-in làm độ khó "chôn màu" biến mất -> level DỄ hơn thiết kế gốc
  (đúng hướng user muốn: đỡ rối/kẹt). Muốn giữ nhiều xe đôi hơn -> nới KEEP_SPREAD.

15) ENV OVERRIDE build-levels.mjs: TARGET & MAXLAYERS & TRACK (2026-07-25)
────────────────────────────────────────────────────────────────────────────
Thêm 3 env override (theo mẫu SKILL/FILL/BOARDSIZE có sẵn, mặc định KHÔNG đổi):
• TARGET=<n>    -> ép winrate target cho lần --only (thay vì CSV / đường auto).
• MAXLAYERS=<n> -> giới hạn SỐ LỚP VIỀN slime addOuterLayers grow quanh subject
  (mặc định 25 ≈ fill tới FILL). Đặt 2 = subject giữ cỡ + tối đa 2 lớp viền,
  phần còn lại để trống (dùng khi chủ thể bé, không muốn phình viền lấp cả board).
• TRACK=<square|rect|u|...> -> ép track cho --only (thay vì trackFor mặc định).
  LƯU Ý: toàn bộ level tranh (1-120, 200-300, 500+) đang là "square" -> build 1
  level mới NHỚ thêm TRACK=square, kẻo trackFor() trả "u"/"rect" thành cá biệt.
Ví dụ đã build L30 (super-hard, viền mỏng):
  BOARDSIZE=39 TARGET=30 SKILL=0.9 MAXLAYERS=2 TRACK=square node scripts/build-levels.mjs --only 30
  -> L30 = 39x39, SQUARE, 41% fill (rocket + 2 lớp viền), 9 màu, 25 xe
     (2 xe đôi + 1 xe ba). Winrate target 30% -> đo 31% ✓ (square dễ chạm hơn "u";
     bản "u" trước đo 19%). tester đã mô phỏng "bay juggle".

────────────────────────────────────────────────────────────────────────────
17) TRAY MODE + TWO-LAYER SẠCH + PACK L126-185 (2026-07-25)
────────────────────────────────────────────────────────────────────────────
4 CƠ CHẾ MỚI (theo yêu cầu user):
• XE "?" CHÔN: CHỐT (2026-07-26) = KHỐI XANH NHẠT ĐẶC đồng nhất (setTintFill
  BURIED_TINT=0xa9d0f0) che HẲN màu thật + số ghế — mọi xe "?" nhìn giống hệt,
  KHÔNG đoán được màu. Hiện MÀU THẬT (clearTint + alpha 1, revealBuried) chỉ khi
  xe LÊN ĐẦU CỘT hoặc user CLICK đưa lên khay/ray. (Trước thử "màu mờ" rồi "siêu
  nhạt" nhưng user muốn giấu hẳn.)
• NHÓM XE (đôi/ba): CHỐT = đặt ĐẦU-TRƯỚC (pickGroups quét front→deep, đặt ở HÀNG
  NÔNG NHẤT còn solvable) -> xe đôi/ba xuất hiện SỚM, nhìn thấy dây ngay; cặp
  cùng-hàng-sâu bị TÁCH DỌC khi cột vơi không đều nên phải đặt gần đầu. Ưu tiên
  cùng hàng; fallback early cross-row (3 hàng đầu). ⚠ Fix này nằm trong THỨ TỰ XE
  (dữ liệu) -> chỉ áp khi REBUILD level (đã fix code, chờ user OK build lại pack).
  ⚠ ĐÃ SỬA BUG distinctify: revert cũ chỉ khôi phục vị trí nhóm, không khôi phục
  ô bị swap -> nhân bản/mất xe -> sập bảng màu (5 màu top -> xe 1-2 màu ->
  unsolvable). Giờ snapshot TOÀN BỘ order.
• SLIME "?" gom thành MẢNG liền (BFS blob) thay vì rải rác — makeHidden.
• TWO-LAYER SẠCH (thay L2RICH rải màu lung tung, xấu): makeTwoLayerPicture —
  tô chủ thể X màu -> TOP = giảm còn Y màu (sạch, đẹp), ĐÁY (layer2) = các màu
  chi tiết bị gộp đi (X−Y màu, mạch lạc vì là ảnh thật). Bảng màu top/đáy RỜI
  nhau -> tổng X màu "trong cuộc" nhưng nhìn chỉ Y. CSV cột 18 "l1"=Y (env L1COLORS).
  Tuner GHIM top (không giảm màu), chỉ sweep số xe + nhóm.

TRAY MODE (khay 1 chiều) — CSV cột 19 "tray"=1 (env TRAY), Level.tray:
• Bấm xe hàng xếp -> vào Ô KHAY trống kế (1→5), KHÔNG ra thẳng ray. Xe khay tự
  phóng khi màu lộ, ăn, rồi VỀ ĐÚNG khay (giữ chỗ), chỉ rời khi hết ghế. KHÔNG
  rút xe ra / KHÔNG juggle. Thua = 5 khay đầy xe kẹt màu + không xe nào chạy.
  Game: reuse autoRelaunchBays (mở khóa AUTO_CIRCLE), traySlot giữ chỗ, checkTrayStuck.
• Bot: playAverage nhánh tray (tất định hơn nhiều -> ĐO WINRATE CHÍNH XÁC như
  user muốn). solvable() sẵn có = đúng model tray (không juggle).
• ⭐ PHÁT HIỆN: CÙNG 1 level, tray đo ~8% còn non-tray đo ~43% — bot juggle làm
  non-tray DỄ hơn nhiều. Nên 2 mode cần params KHÁC cho cùng target:
  NON-TRAY = 5 line + đáy nhiều màu (l1 thấp) [5 line cần cho solver sống với
  đáy nhiều màu]; TRAY = 4 line + đáy 2 màu (l1 cao). Difficulty non-tray xuống
  10% rất khó (juggle quá mạnh) — non-tray super hay dừng ~15-23%.

PACK L126-185 (60 level, N_LEVELS=185): mỗi mode 30 = 10 super(10%)+10 hard(25%)
+10 tb(35%) @0.9, mỗi loại 5 ảnh × 2 cỡ (31,35). Non-Tray L126-155, Tray L156-185.
Ảnh xen-kẽ (train/pencils/robot/dino/star/airplane/bus/fish/cat/owl/bear/mushroom/
elephant/goldfish/spacecat). gen-pack-rows.mjs sinh CSV; cột 20 "img" gán ảnh/level.
• KQ (40 lượt, đúng mode+lane): 60/60 SOLVABLE. Tray bám target tốt (super 3-15,
  medium 30-53). Non-tray spread rộng hơn, vài level dễ (L127 48, L135/148/149 68-80).
• ⚠ SOLVABILITY two-layer LƯỢNG TỬ + theo ẢNH: l1 thấp 1 nấc = wedge, cao 1 nấc =
  dễ; không có mức giữa. super-35 cần l1 5-6; medium 31 cần l1 6 vài ảnh. l1 mỗi
  level đã ghi vào CSV cột 18 (không phải out-of-band).
• CÒN LẠI: seed-sweep các outlier non-tray (127,135,148,149...) nếu muốn sát target;
  bot 0.9 vốn nhiễu -> PLAYTEST của user là thước đo cuối.
CHỖ CHỈNH: makeTwoLayerPicture / makeHidden / pickGroups(early+restore) / playAverage
  (nhánh tray) trong build-levels.mjs; GameScene (trayMode/parkChest/relaunchFromSlot/
  autoRelaunchBays/checkTrayStuck/BURIED_TINT); level.ts (lanes/tray). set-l1.mjs sửa l1.

────────────────────────────────────────────────────────────────────────────
18) TRAY → "CHẠY THEO LÔ" (BATCH) — REDESIGN (2026-07-26)
────────────────────────────────────────────────────────────────────────────
Ý TƯỞNG (user): 1 LÔ xe đi đón slime, KHÔNG phải từng xe tự phát như cũ.
• BỎ auto-fire. Xe stage vào khay (1→5) rồi NẰM YÊN. Có 1 NÚT "GO" (placeholder,
  design sau) → phóng CẢ LÔ (mọi xe trong khay) ra ray cùng lúc.
• CHAIN: lô vòng ray như 1 đội, CỨ CHẠY TIẾP chừng nào CÒN ≥1 xe trong lô ăn được
  (xe A bóc vòng ngoài → lộ màu xe B → B ăn) — tới ĐIỂM BẤT ĐỘNG (không xe nào
  active ăn được nữa) thì cả lô dừng.
• Kết thúc lô: xe HẾT GHẾ rời sân; xe CÒN KẸT VỀ ĐÚNG KHAY reserve (chờ lô sau).
• KHOÁ khi chạy: khay đóng, xe dưới không nhảy lên, nút GO tắt (batchRunning).
• THUA: 5 khay đầy xe kẹt + không stage thêm được + GO không ăn được gì (deadlock).
• ⭐ TẤT ĐỊNH HƠN cũ (kết quả 1 lần GO = điểm bất động, thứ tự trong lô vô can) →
  ĐO WINRATE VẪN CHÍNH XÁC.
CỜ ROLLBACK: TRAY_BATCH=true (GameScene.ts) — false = về auto-fire tray cũ (mục 17).
CHỖ CHỈNH: GameScene.ts (TRAY_BATCH, buildGoButton/positionGoButton/updateGoButton,
  launchBatch, canKeepCircling[batch], endBatchIfDone, checkTrayStuck[batch], khoá
  launchFromInventory, updateSlotWarning). build-levels.mjs playAverage nhánh tray =
  vòng STAGE(fill khay skill-gated) → GO(fixed-point collect mọi xe khay) → lặp.
• ĐÃ VERIFY: L156 stage→GO→cả lô 4 xe ra ray (signal 4/5), khay khoá, GO tắt, không
  lỗi console.
• 🐛 FIX (2026-07-26): xe rớt LẺ khỏi lô giữa chừng về khay (ảnh user: blue kẹt khay
  khi 2 xe khác chạy). Gốc: canKeepCircling xét ảnh-chụp-tức-thời → 1 xe chạm cuối
  vòng đúng lúc tạm-thời-không-với-tới thì đỗ sớm. SỬA: xe trong lô KHÔNG đỗ lẻ nữa —
  canKeepCircling batch = luôn chạy khi batchRunning; cả lô đỗ ĐỒNG LOẠT khi STALE
  (không xe nào bắn suốt BATCH_END_GRACE=1600ms) qua endBatchStaleCheck + batchLastProgress
  (reset trong tryShoot). Verify: 2 xe blocked chạy chung tới stale rồi cùng về khay.
• 🐛 FIX booster ADD (2026-07-26): (1) nút GO NHẢY SANG TRÁI khi Add làm khay rộng
  (6 khay ~ kín ngang, hết lề) → đổi positionGoButton về TOẠ ĐỘ CỐ ĐỊNH (góc phải,
  PHÍA TRÊN hàng khay, trong dải xanh board↔khay), không theo hình học khay nữa.
  (2) XE CUỐI xuất phát chậm nhiều: MAX_ON_TRACK=5 chặn, khay>5 thì xe thứ 6 chờ ghế
  (mà xe batch không rời tới hết lô) → xe 6 chỉ chạy SAU khi 5 xe đầu xong. SỬA:
  onTrackCap()=max(5,slotCount) trong batch → cả lô lên ray cùng lúc (trySpawn + signal).
• 🐛 FIX stage đè ô + dồn khay (2026-07-26): xe stage NHẢY ĐÈ ô đang có xe (do tính
  start=SỐ-ô-có-xe rồi đặt vào ô đó, sai khi có GAP giữa) → đổi parkIntoSlot theo
  findIndex(ô-trống-đầu). VÀ khi lô VỀ khay: dồn xe SÁT TRÁI (endBatchStaleCheck park
  left-packed 0,1,2… bỏ traySlot cũ) → khay luôn liền mạch, không gap → hết đè.
• ✅ RÀ SOÁT + XE ĐÔI/NHÓM: test nhóm-3 L156 (14/43/20) — stage/launch/return đều
  ADJACENT đúng thứ tự; member consecutive trong active nên luôn packed cạnh nhau;
  nhóm chỉ rời khi mọi member đầy nên không có "nửa nhóm". Không bug. (Ảnh chụp giữa
  lúc glide track→khay ~950ms trông "lơ lửng nối dây" nhưng về đúng ô — chỉ là animation.)
• ✅ RE-TUNE XONG (2026-07-26): rebuild cả 30 level tray L156-185 (--only, config
  winratedesign1.csv) → tuner tự sweep lại SỐ XE theo bot batch mới → 24/30 sát target
  ngay. 6 outlier (L162 unsolvable, L167/173 quá dễ, L169/178/180 quá khó) sửa bằng
  set-l1.mjs (l1 per-level: tăng=dễ, giảm=khó) rồi rebuild → 30/30 SOLVABLE, winrate
  @0.9: super~0-18%, hard~15-40%, medium~28-40% (đa số sát target). L167(40)/L169(15)
  còn lệch nhẹ do l1 nhảy bậc "lượng tử" — trong ngưỡng nhiễu bot; playtest = thước đo cuối.
  ⚠ Bước tune l1 nằm trong CSV Manythings/Design winrate/winratedesign1.csv (build input).

────────────────────────────────────────────────────────────────────────────
19) ROCK-WALL LOGIC LEVELS (chặn cạnh = U/arch/line) (2026-07-26)
────────────────────────────────────────────────────────────────────────────
Ý user: các level pack tuy khó nhưng (a) DÀI LÊ THÊ (board to 31/35 + 2 lớp),
(b) không có level SIÊU khó thật, (c) qua được nhờ NHANH TAY hơn LOGIC. Giải:
dùng "slime đá" (đá cứng) CHẶN 1-3 CẠNH của board vuông -> frontier hẹp như chữ
U / U-ngược / line -> ít màu lộ cùng lúc -> phải TÍNH thứ tự bóc (LOGIC), board
nhỏ lại nên KHÔNG lê thê.

CƠ CHẾ (tách nhánh git "rock-walls" vì main đang có TRAY_BATCH dở của session khác):
• placeWalls(board, cols, rows, edges, thick): ghi ĐÁ CỨNG (code OBST=90) lên
  hàng/cột ngoài cùng của các cạnh trong `edges` (chuỗi T/B/L/R). Đặt TRƯỚC
  two-layer split + TRƯỚC tune. CSV cột 21 "walls" (vd T, TL, TLR) / env WALLS,
  WALLTHICK. Track vẫn "square" (xe chạy 4 cạnh) — ĐÁ chặn tia nên frontier hẹp.
• SIM HIỂU VẬT CẢN: thêm const OBST=90 + isColor(v)=0..89. Sửa để rock KHÔNG bị
  đếm/không thu/CHẶN tia: slimeCount, distinctColors, allocateCars, exposedTiles
  (add vào S chỉ ô màu; rock vẫn chặn qua reach[]), peelLayers/solvable/playAverage
  remaining, reduceColors/ensureColors/colorOuterness (bỏ qua rock), boardToPng
  (rock = xám). GAME đã sẵn xử lý đá cứng (makeObstacle "rock-hard", rayHit dừng ở
  key, isRemovable loại đá khỏi điều kiện thắng) — KHÔNG cần sửa game cho walls.
• SLIME "?" ĐỔI SANG TRẮNG (user): makeHiddenKey body tint 0xf3f8ff (trắng tươi) +
  "?" xanh #2f8fd8 viền trắng (trước là đen).

2 BỘ 15 LEVEL (winrate 5-25% @0.9, non-tray):
• L301-315 CÓ TƯỜNG: 40% chặn TOP-only (301-306), 5 level 2-cạnh (307-311),
  4 level 3-cạnh=line (312-315). 40%+ là 25×25, còn lại 31/35. 15/15 solvable.
  Winrate đo: 10/38/38/33/18/45/18/13/23/20/33/13/3/10/3 (star lì -> 302/303/306
  hơi cao; 3-cạnh xuống thấp tốt). gen-wall-rows.mjs sinh CSV; l1/lanes/bury/walls
  ghi trong CSV (L303/304 đã nudge: 303=l1 5/3line/bury.7, 304=l1 7/5line).
• L316-330 KHÔNG TƯỜNG, 25×25, CÙNG dãy target — độ khó từ chôn+two-layer+ít line
  thay vì tường (bộ đối chứng user yêu cầu). Khó xuống 5-10% hơn (không có tường
  thu hẹp frontier) -> dùng 3 line + l1 thấp (đáy nhiều màu) + chôn dày.
CHỖ CHỈNH: placeWalls + OBST/isColor + gen-wall-rows.mjs + gen-nowall-rows.mjs
  (build-levels.mjs); makeHiddenKey (GameScene). N_LEVELS=330, LEVEL_COUNT=330.
⚠ NHÁNH RIÊNG: đã làm ở git worktree branch "rock-walls" để không đè TRAY_BATCH của
  session kia; ĐÃ MERGE vào main 2026-07-26 (auto-merge sạch: TRAY_BATCH giữ L156-185
  re-tune, rock-walls thêm L301-330 — 2 vùng rời nhau).

════════════════════════════════════════════════════════════════════════════
20) GIAO DIỆN NHÌN "ĐẸP HƠN" — BOARD TỐI + TILE FACELESS + NỀN TRANH (2026-07-26)
════════════════════════════════════════════════════════════════════════════
MỤC TIÊU (so game tham chiếu): game họ đẹp hơn KHÔNG do art xịn hơn mà do TƯƠNG
PHẢN + bảng màu + độ SẠCH. 4 rule dưới đây là cách áp cho game mình.

RULE 1 — BOARD NỀN TỐI (mặc định) để pixel-art NỔI:
• Mặt board (mat) = navy đậm 0x2b2f4a (trước là cát 0xdcc79c). Highlight 0x3a4066,
  ô caro xen kẽ 0x242840. Nền tối → tile màu tương phản value cao → tranh "pop".
• CHỖ CHỈNH: GameScene.buildGroundMat(). Có rẽ nhánh theo cờ per-level (RULE 4).

RULE 2 — TILE BOARD FACELESS (bỏ mặt cười trong "tranh"):
• PNG slime có mặt vẽ sẵn → mảng mosaic bị NHIỄU. Nay tile board vẽ bằng texture
  PHẲNG bo góc (makeTileTexture → "tile-<id>"), CHỈ 2 mắt rất nhỏ & mờ
  (r=size*0.032, alpha 0.28) cho có chút sức sống. KHÔNG miệng, KHÔNG mặt đầy đủ.
• Mặt CƯỜI chỉ còn trên XE/CHEST ở khay (car-*.png) + slime chạy (runner) — giữ
  chất "nhân vật lên xe". Tile ẩn "?" dùng "tile-hidden" (trắng phẳng) + "?" xanh.
• CHỖ CHỈNH: GameScene.makeTileTexture / makeKey / makeHiddenKey / reveal.
  (chỉnh alpha mắt 0.28 lên/xuống nếu muốn rõ/mờ hơn; =0 là phẳng hẳn.)

RULE 3 — RUNNER GIỮ KÍCH THƯỚC CHUẨN trên board lớn:
• Board tranh > 25 thì this.cell nhỏ lại (để vừa đường) → slime chạy ra xe bị nhỏ.
• Thêm this.stdCell = cell mà board 25×25 sẽ có trong CÙNG layout. updateRunners
  nhân scale runner với sizeComp = stdCell/cell → slime gần xe LUÔN bằng size như
  level 25×25. Board ≤25: stdCell==cell → sizeComp=1, không đổi.
• CHỖ CHỈNH: GameScene buildBoard (tính stdCell) + updateRunners (base * sizeComp).

RULE 4 — NỀN CỦA "TRANH" (đừng để mảng TRẮNG ĐỤC):
• Lỗi hay gặp: level tranh lấp KÍN cả nền bằng ô trắng/xám nhạt (id 8/9/10) →
  trên board tối thành mảng "trắng đục" xấu, không pop không lùi.
• Cách xử lý (chọn theo level):
   (a) BỎ NỀN: nền tranh = ô trống (-1) → lộ board, chủ thể "float" (giống ref).
       Dùng build-one.mjs chế độ bỏ nền / editor "Nền: bỏ".
   (b) NỀN TỐI TRUNG TÍNH: remap ô nền (vd 9,10) → id 12 (#262630 navy-đen). Vẫn
       có tile để ăn nhưng hoà vào board → chủ thể nổi. (đã làm cho L303.)
   (c) 2 tông tối cho có chiều sâu (nền id 12 + viền/shadow id 10).
• CỜ PER-LEVEL "lightBoard": true (Level.lightBoard) → board đó dùng MẶT SÁNG (cát)
  thay navy. Kết hợp nền tranh TỐI (id 12) → sao/chủ thể trên panel tối, KHUNG board
  sáng bao ngoài (kiểu "tranh nền đen, khung gỗ sáng"). L303 = ví dụ mẫu.
  ⚠ getLevel() copy field TAY → field mới PHẢI thêm vào src/game/level.ts (đã thêm
    lightBoard); quên là cờ bị rớt, đổi JSON không có tác dụng.

• ⭐ RULE MỚI THAY THẾ (2026-07-26, user): BOARD LUÔN CARO TỐI + NỀN TRANH SÁNG.
  Bỏ hẳn cơ chế lightBoard/tự-chọn-board-sáng ở trên. Thay bằng:
  - BOARD MAT: LUÔN caro tối (navy 0x2b2f4a xen 0x323858) — không còn board sáng.
    (lightBoard không set nữa; field bị bỏ khỏi mọi level trong designed.json.)
  - NỀN TRANH (bg fill) = 1 MÀU SÁNG (mặc định XANH DA TRỜI id15). Nếu chủ thể ĐÃ
    dùng id15 → chọn màu sáng khác KHÔNG trùng (ưu tiên teal 4 / blue 5 / peach 17 /
    yellow 2…, danh sách BRIGHT_BG_PREF). Chọn màu né chủ thể nên KHÔNG bao giờ gộp
    → chủ thể (kể cả chủ thể TỐI như chim cánh cụt) nổi trên panel sáng thay vì chìm.
  - Vì sao viền đen hết chìm: trước để chủ thể "float" trên board tối → viền đen id10/
    12 lẫn vào navy. Nay chủ thể nằm trên PANEL SÁNG → tối trên sáng luôn rõ nét.
  - CHỖ CHỈNH BUILD: choosePictureTheme() trong build-levels.mjs (đã rút gọn: luôn
    lightBoard=false, bgId = màu sáng đầu tiên trong BRIGHT_BG_PREF mà chủ thể chưa
    dùng). PIC_BG=<id> vẫn override thủ công.
  - MIGRATE 45 LEVEL CŨ: scripts/migrate-bright-bg.mjs — dò bgId mỗi level (mode viền
    bbox), RELABEL 1:1 bgId → màu sáng chưa dùng trên board+layer2+hidden+chests, bỏ
    lightBoard. Đổi tên 1 lớp màu song ánh → winrate/độ khó GIỮ NGUYÊN TUYỆT ĐỐI.
    Đã chạy cho L1-45 (đa số → 15; L17/20/21/40→5, L6/27/35/37→17, L23→4).
  - (LỊCH SỬ, đã bỏ) Cơ chế cũ "viền tối→nền sáng+lightBoard" + audit-dark-outline.mjs
    chỉ còn để tham khảo; rule caro-tối/nền-sáng ở trên thay thế toàn bộ.

CHỌN ART CHO LEVEL TRANH (để hợp board tối):
• Chủ thể MÀU SÁNG, ít màu, tách nền rõ, silhouette dễ nhận = tốt.
• TRÁNH viền ĐEN dày quanh chủ thể trên board TỐI (viền đen chìm vào nền, mất nét).
  Nếu art có viền đen: bỏ/đổi viền sáng khi import, HOẶC để level đó lightBoard.

21) REDESIGN 45 LEVEL ĐẦU TỪ FILE THIẾT KẾ + INTRO ĐÁ CỨNG (2026-07-26)
────────────────────────────────────────────────────────────────────────────
• File thiết kế: "Manythings/Design winrate/winratedesign1.csv" — user tự điền
  45 dòng THEO FORMAT NGƯỜI ĐỌC:
    lvl,tier,target,max màu,maxxe,minxe,xedoi,track,max slim,slime_ref,win_ref,
    skill,xe_ref,màu_ref,kích thước(vd "25x25"),Có đá cứng?(Có/Không)
  → build-levels.mjs loadConfig() giờ ĐỌC ĐƯỢC CẢ 2 FORMAT (tự nhận diện qua ô
    kích thước dạng "AxB"): format thiết kế NÀY + format build cũ (21 cột).
  • tier (easy/hard/super) → độ khó (thắng ô "packDiff" tính theo số level).
  • "Có đá cứng?"=Có → TỰ suy ra viền tường theo tier: easy=T (chỉ mép trên),
    hard=TL (2 mép), super=TLR (3 mép). Hàm wallEdgesForDesign().
  • max slime = CHỈ tính layer 1; nếu layer 2 có slime thì vượt cap 1 chút là OK.
• LUẬT MỞ CƠ CHẾ THEO LEVEL (user 2026-07-26), áp cho n≤45:
  - Xe chôn "?" + slime 2 lớp: mở từ L15 (layer2FracFor, buryForDesign).
  - Slime "?" (hidden): mở từ L21 (hiddenFracFor).
  - Đá cứng: từ L23 (level đầu có đá theo thiết kế).
  - Các FEATURE độ khó (2-layer/chôn/"?") gate THEO TARGET, không theo tier:
    target càng thấp → 2-layer càng dày (real slime = friction) để level easy-tier
    nhưng target thấp vẫn có độ khó. Riêng L5/L10 (hard sớm, target≤50) được chôn
    xe từ L5 (nới luật) — chôn xe là PERCEPTION-ONLY (không đổi winrate đo được,
    chỉ tăng khó cho NGƯỜI).
• INTRO ĐÁ CỨNG (GameScene.ts): modal tiếng Việt "Đá Cứng!" hiện 1 lần ở level
  đầu tiên có đá cứng (L23), dedupe bằng localStorage "pf_rock_intro". Giống
  intro xe đôi (TWIN_INTRO_LEVEL). Nội dung: xe không gắp được slime nếu có đá
  cứng chắn đường.
• GIỮ NGUYÊN TUTORIAL: build lại board cho HỢP winrate/nền mới, NHƯNG các intro/
  tutorial (L1 hướng dẫn, L8 xe đôi, booster L6/11/16/21) vẫn chạy vì kích hoạt
  theo SỐ LEVEL, không phụ thuộc board.
• WALL_TEXTURE (GameScene.ts): skin đá tường = "rock-hard"(xám) / "rock-soft"(nâu)
  / "rock-soft-cracked". Đá 2×2 (BIG_HARD=95) render cell*2. (thử nghiệm L303.)
• Rebuild 1 level ở size khác: BOARDSIZE=26 node build --only N (ép size cho --only).
• Đo winrate ổn định: mặc định trials=100 (trước 40); env TRIALS override. Skill=1
  → luôn 0/100% (chỉ đo "có qua nổi"), muốn đo độ khó phải skill<1.
• TĂNG MÀU CHO LEVEL QUÁ DỄ (2026-07-26): board ít màu → xe khớp dễ → winrate cao.
  - ĐỪNG rắc màu ngẫu nhiên (ensureColors speckle): tạo ô màu lẻ KHÔNG gắp được
    (outside-in không tới) → level BẤT KHẢ THI. Đã thử COLORS=12 → 4/5 unsolvable.
  - CÁCH ĐÚNG = ĐỔI CHỦ THỂ sang ảnh NHIỀU MÀU THẬT (vùng màu liền mạch → solvable):
    IMG=<path ảnh> COLORS=<N> node build --only <lvl>. COLORS= giờ NÂNG TRẦN
    MOSAIC (Ksub) để ảnh giữ đủ màu dù max-màu thiết kế thấp (KHÔNG còn speckle).
  - Đã fix L6/L9 (image-swap) & thêm 7 ART MỚI 10-11 màu (user tạo Nano Banana,
    2026-07-26): 11_rocket_toys, 11_train_toys, 11_robot_heroes, 10_monster_heroes,
    10_parrot_animals, 11_parakeet_animals, 10_macaw_animals (ở sliced/_superhard).
    Fix được L10 (37%), L34 (82%), L42 (52%), L25 (16% solvable, tường T).
• BÀI HỌC QUAN TRỌNG: nhiều màu CHƯA đủ để khó. Độ khó cần MÀU XEN KẼ ở frontier.
  Clip-art sạch = MẢNG MÀU TO LIỀN → mỗi màu gắp 1 lượt hết → vẫn dễ, NHẤT LÀ board
  nhỏ 25×25. Vì vậy L5/L6/L9 (25×25, chủ thể mảng to) VẪN dễ (100/98/74%) dù 9-11 màu.
  Board to (30-31) + chủ thể chi tiết (robot/train/macaw) thì fix được.

22) ÉP LEVEL DỄ VỀ ĐÚNG TARGET BẰNG ẢNH CHIA-MẢNH + TUNER CHÍNH XÁC (2026-07-27)
────────────────────────────────────────────────────────────────────────────
• Vấn đề: chủ thể MẢNG TO (đồng hồ/xe buýt/quái vật) trên target-mới-thấp (khó) →
  chôn xe (order-bias) cỡ nào cũng ~100% → không xuống nổi target. CÁCH ĐÚNG =
  ĐỔI sang CHỦ THỂ ĐƠN NHIỀU MẢNH MÀU NHỎ XEN KẼ (con vật chia mảng, giỏ trái cây)
  → frontier nhiều màu → tuner có dư địa ép khó. Mảng to (bạch tuộc thân tím, robot
  khối) vẫn dễ. Fix: L12→turtle(85→93), L25→fruitbasket(60→59 KHỚP), L27→rainbowfish
  (80→75), L33→rocket_rainbow(85→89, đẹp hơn tên lửa đỏ trơn).
• ẢNH MỚI (user thả vào public/art/level art/4, 2026-07-27) → cắt vào sliced/_superhard
  prefix 11_: sinh vật biển (octopus/fruitbasket/rainbowfish/turtle/whale/crab/…),
  khủng long (11_*_dino), ảnh đơn (rocket_rainbow/house_colorful/robot_office/
  tower_blocks), holiday-nền-checkerboard (11_*_holiday — TRÁNH: pipeline giản lược
  checkerboard → ít xe → dễ). Cắt bằng sharp: extract ô lưới → trim trắng → bỏ ô
  trống (stats stdev thấp). Ảnh nền TRẮNG: rule nền-sáng có thể rơi về trắng id8
  (chủ thể đã dùng hết id sáng hơn) — OK, trắng vẫn nổi trên board caro tối.
• ⚠ TUNER NHIỄU: tuneToTarget dò candidate ở 16 trials (mặc định) → chốt "đúng target"
  nhưng đo lại 100 trials lệch ±15 (vd L29 tuner-nghĩ-75 → thực 52 QUÁ KHÓ). FIX:
  env TUNE_TRIALS=80 (nâng trials tuner) + lưới bias mịn 9 mức → re-tune 7 outlier về
  sát target (L29 52→80, L22 97→88, L14 75→88). Chậm hơn nhưng chuẩn. Quy trình đo
  chuẩn = --report (100 trials, grade designed.json thật), KHÔNG tin số build-time.
• ⚠ RULE ẢNH XẤU (user): L3-35 ảnh nào "xấu quá" (1 tông) thì thay ảnh mới — NHƯNG
  level target 100 (showcase dễ: L21 đèn, L26 sư tử, L31 khỉ) PHẢI giữ ít màu, không
  swap ảnh nhiều màu (sẽ phá độ dễ). Chỉ swap ở level target < 100.
• GIỚI HẠN CÒN LẠI: L5 (target 50 ở level 5 quá sớm + board nhỏ → nên nới ~85),
  L6/L9 (easy-tier, hơi dễ nhưng chấp nhận được). Muốn khó hơn: board to hơn, hoặc
  art CHI TIẾT (nhiều mảng nhỏ xen kẽ, không phải mảng to). Đòn bẩy chắc = tường đá.

23) REVIVE = THÊM Ô CHỜ (2026-08-02)
────────────────────────────────────────────────────────────────────────────
• Thua (queue tràn / tray kẹt) → màn "Out of Space" có nút REVIVE (900 vàng).
  ĐỔI HÀNH VI: trước đây revive DỌN SẠCH 5 ô chờ (xe đỗ bay đi hết); giờ revive
  THÊM 1 Ô CHỜ (hàng bay rộng ra), xe đang đỗ GIỮ NGUYÊN, xe vừa bị tràn được
  đỗ vào ô mới → chơi tiếp. (GameScene.showLoseModal → revive)
• Xe đôi/xe 3 làm tràn: thêm ĐỦ ô cho cả nhóm (need − free, tối thiểu 1) —
  tránh trả 900 vàng xong thua lại ngay vì nhóm vẫn không đủ chỗ.
• CHỈ 1 LẦN/VÁN (user 2026-08-02): cờ revivedThisAttempt (reset trong
  startLevel — Replay/level mới lại có 1 lượt). Thua lần 2 sau khi đã revive →
  ẨN HẲN nút revive (kể cả bản xám "Need 900"), chỉ còn Replay | Home (hàng nút
  dồn lên chỗ nút revive).
• GIÁ HIỂN THỊ = COIN VẼ (đĩa vàng 0xf9c22e viền 0xc98a10, giống HUD/booster
  bar) — bỏ emoji 🪙 vì trên Windows nhìn như VIÊN ĐÁ xám (user 2026-08-02).
  Sửa luôn dòng "+50" màn thắng + toast mua booster ("900 gold").
  layoutSlots co pitch khi hàng > 6 ô để mọi ô vẫn nằm gọn màn 480px (≤6 ô
  pitch giữ nguyên như cũ). Booster "Add" vẫn cap 6 ô như trước.
• Tray-stuck lose (không có xe pending): revive vẫn +1 ô trống → stage thêm
  1 xe từ hàng đợi để tự gỡ kẹt.

24) TIM (LIVES) — THUA TRỪ 1 TIM, 0 TIM KHÔNG ĐƯỢC CHƠI (2026-08-02)
────────────────────────────────────────────────────────────────────────────
• src/game/hearts.ts: MAX_HEARTS=5, sàn 0. Lưu pf_hearts + pf_hearts_ts.
  HỒI TIM: +1 mỗi 20 phút (HEART_REGEN_MS), tính LƯỜI từ timestamp khi đọc —
  không cần đồng hồ chạy, sống qua reload. Đầy 5 thì đồng hồ nghỉ.
• THUA 1 LEVEL = -1 TIM: trừ khi user rời màn thua qua Retry/Home (revive thì
  KHÔNG mất tim — ván còn sống). Animation (GameScene showLoseModal →
  exitWithHeart): tim to (art/heart.png) pop giữa màn, "-1" bay lên, tim xám
  dần + chìm xuống, kèm hàng 5 tim (đỏ = còn, xám = mất) rồi mới chuyển màn.
• 0 TIM = KHÔNG ĐƯỢC CHƠI TIẾP:
  - Màn thua: nút Replay XÁM (chỉ còn Home) + dòng "Out of hearts — next in ~Xm".
  - Map (LevelSelectScene.tapAt): tap level → nhịp deny đỏ + toast đếm phút.
  - GameScene create: vào thẳng bằng ?level=/refresh với 0 tim → đá về map.
• Pill ❤ trên Home nối SỐ THẬT: "N + MAX" khi đầy, "N + Xm" khi đang hồi.
• BỎ NGANG CŨNG MẤT TIM (user 2026-08-02): Settings trong ván → nút "🏠 Home"
  (đổi từ "🗺 Levels") KHÔNG về thẳng nữa — hiện confirm "GO HOME? Quitting
  this level costs 1 ❤" (STAY / Leave -1 ❤). Đồng ý mới trừ tim + chạy beat
  "-1 ❤" (playHeartLoss — dùng chung với màn thua) rồi mới về map.
  playHeartLoss set tutPaused để đóng băng ván trong lúc beat chạy.
• ?reset=all xoá pf_hearts/pf_hearts_ts (prefix pf_) → về 5 tim. Chưa có mua
  tim bằng vàng/xem ads — thêm sau nếu cần.

25) NHỊP CHỜ NHANH HƠN + VIỆT HOÁ UI (2026-08-02)
────────────────────────────────────────────────────────────────────────────
• NHỊP CHỜ (beatMs, GameScene): mọi khoảng "chờ 1 nhịp" quanh thắng/thua/trừ
  tim chạy NHANH HƠN thiết kế gốc — ×2/3 bình thường, ×1/2 ở 10 LEVEL ĐẦU
  (user: "3s → 2s; 10 level đầu 1.5s"). Áp cho: hold trước màn thắng (750→500/
  375ms), hold trước màn thua (650→433/325ms), toàn bộ beat "-1 ❤"
  (1300→867/650ms). KHÔNG áp cho nhịp tutorial (700ms chờ xe đỗ) — đó là nhịp
  dạy, không phải loading.
• NGÔN NGỮ UI: toàn bộ câu chữ hiển thị đổi sang TIẾNG VIỆT, GIỮ NGUYÊN từ
  thông dụng: Level, Home, Revive, Coin, GO, MAX, Booster + tên booster
  (Add/Grab/Shuffle/Magnet), Settings, Sound FX, Close, Replay, TAP TO START,
  Nice!/Great!/Wow!/Cool!. Tiền gọi là COIN (rewardLabel: "Gold"→"Coin").
  Sweep: tutorial L1/xe đôi/booster, toast/notice, màn thắng/thua, confirm về
  Home, Settings map (Tuần tự/Tự do, Nhảy tới Level), event Clover (Quà Đặc
  Biệt, Đã nhận). Intro Đá Cứng vốn đã tiếng Việt (§21).

26) ĐÓNG GÓI APK ANDROID (Capacitor) (2026-08-02)
────────────────────────────────────────────────────────────────────────────
• Đã cài @capacitor/core+cli+android v6; capacitor.config.ts: appId
  "com.hopin.game", appName "Hop In!", webDir "dist". Thư mục android/ đã sinh.
  vite.config base:"./" nên asset chạy được trong WebView (không cần sửa).
• TOOLCHAIN CÀI CỤC BỘ (không cần admin, không đụng PATH hệ thống) ở
  C:\CuongPC\android-tools\ : jdk\jdk-17.* (Temurin 17) + android-sdk\
  (cmdline-tools, platform-tools, platforms;android-34, build-tools;34.0.0).
  android/local.properties trỏ sdk.dir tới đó.
  ⚠ local.properties PHẢI dùng dấu / (hoặc \\): file .properties coi \ là ký tự
  escape → "C:\CuongPC\..." bị hỏng thành "C:CuongPC..." → build lỗi
  "IOException: The filename, directory name, or volume label syntax is
  incorrect". Đây là lỗi đã vấp 1 lần.
  ⚠ sdkmanager --licenses KHÔNG nhận được "y" qua pipe (stdin bị chặn) → ghi
  thẳng file hash vào android-sdk\licenses\ (android-sdk-license,
  -preview-license, -arm-dbt-license).
• LỆNH BUILD = **npm run apk** (scripts/build-apk.mjs làm hết): npm run build
  (web -> dist/, optimize-art 94MB -> 3.2MB) → tạo android/ nếu chưa có (cap add)
  hoặc cap sync nếu có rồi → ghi local.properties → chạy setup-android.mjs →
  gradlew assembleDebug → copy ra HopIn-v0.0.<số commit>-debug.apk ở gốc repo
  (~7.7MB). Tự dò JAVA_HOME/ANDROID_HOME, override được bằng env.
  ⚠ ĐỪNG chỉ chạy gradlew: phải build lại web + sync, không thì APK vẫn ôm bản
  web cũ.
• KHÔNG TRACK android/ (user 2026-08-02): cả thư mục là sinh tự động →
  .gitignore bỏ android/ + *.apk + *.aab. Thứ ĐƯỢC track (đủ để dựng lại y hệt):
  capacitor.config.ts, scripts/setup-android.mjs, scripts/build-apk.mjs, dep
  @capacitor/* trong package.json. Xoá android/ đi chạy npm run apk là ra lại.
• scripts/setup-android.mjs = mọi tuỳ biến tay trên thư mục sinh tự động (chạy
  lại được nhiều lần, idempotent): icon + khoá portrait trong AndroidManifest.
  Sinh toàn bộ launcher icon từ art game
  (public/art/slime3D.png, ô giữa = slime xanh cyan) — cắt nền trắng bằng luật
  "sáng VÀ không bão hoà" (giữ được highlight trắng của khối slime), đặt trên
  nền gradient navy #3d4370→#20233a. Sinh cả 5 mật độ: ic_launcher (bo góc),
  ic_launcher_round (tròn), ic_launcher_foreground (adaptive, vùng an toàn 56%)
  + drawable/ic_launcher_background.xml. Chạy lại sau khi đổi art rồi rebuild.
• APK hiện tại = DEBUG (ký bằng debug key) → cài thẳng vào máy để test được,
  KHÔNG lên Play Store được. Muốn bản release phải tạo keystore riêng + ký
  (chưa làm; build-apk.mjs đã có sẵn cờ --release chờ nối signing).
  versionCode/versionName vẫn mặc định 1 / "1.0" — build stamp hiển thị TRONG
  game (v0.0.<số commit> · hash) vẫn lấy từ git như cũ, không liên quan.

────────────────────────────────────────────────────────────────────────────
GHI CHÚ CHUNG
────────────────────────────────────────────────────────────────────────────
• Stack: Phaser 3 + TypeScript + Vite. Chạy web; sau đóng gói APK bằng Capacitor.
• Kiểm tra biên dịch:  npx tsc --noEmit
• Chạy dev:            npm run dev
• Level đặc biệt test obstacle: mở ?level=999
================================================================================
